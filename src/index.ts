import { CreateModel as createModel } from './core/index' // 导出默认模块
import GlobalProvider from './provider/GlobalProvider'
import connect from './core/connect'
export { createModel, GlobalProvider, connect }
